# Intentionally MISSING .gitignore (fixture for bad_repo)
# This file documents what an ideal .gitignore would include

# NOTE: Do not add a .gitignore here — this is the "bad_repo" fixture.
# The Repo Doctor tool should detect the missing .gitignore and flag it.

# This is a placeholder README for the bad_repo fixture.
# It intentionally has fewer than 50 words so the README check also fails.

Bad Repo
========

A deliberately broken repository for testing Repo Doctor diagnostics.
